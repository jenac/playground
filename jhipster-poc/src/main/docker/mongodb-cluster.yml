version: '2'
services:
    poc-mongodb:
        image: mongo:3.4.10
        ports:
            - "27017:27017"
        command: mongos --configdb poc-mongodb-config
    poc-mongodb-node:
        build:
            context: .
            dockerfile: mongodb/MongoDB.Dockerfile
        command: mongod --replSet rs1 --noprealloc --smallfiles
    poc-mongodb-config:
        image: mongo:3.4.10
        container_name: poc-mongodb-config
        command: mongod --noprealloc --smallfiles --configsvr --dbpath /data/db

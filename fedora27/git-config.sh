#!/bin/bash
git config --global user.name "Lihe Chen"
git config --global user.email "lihe.chen@gmail.com"
git config --global credential.helper cache
git config --global credential.helper "cache --timeout=7200"

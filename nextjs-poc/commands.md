* init project
```bash
mkdir nextjs-poc
cd nextjs-poc
npm init -y
npm install --save react react-dom next
mkdir pages
npm run dev
```

* eslint
```bash
npm install --save-dev eslint eslint-plugin-react react-hooks
```